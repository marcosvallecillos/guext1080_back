{% extends 'base.html.twig' %}

{% block title %}Variables{% endblock %}

{% block body %}
    <h1>Variables</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ variable.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ variable.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_variables_index') }}">back to list</a>

    <a href="{{ path('app_variables_edit', {'id': variable.id}) }}">edit</a>

    {{ include('variables/_delete_form.html.twig') }}
{% endblock %}
